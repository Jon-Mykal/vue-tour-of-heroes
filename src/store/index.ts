import { createStore } from 'vuex'
import * as mdl_heroes from '@/store/mdl_heroes'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    mdl_heroes
  }
})
